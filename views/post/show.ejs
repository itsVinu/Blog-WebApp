<%- include("../partials/header")%>
<%- include("../partials/navbar")%>




    <div class="container card" >
      <%- include("../partials/flash")%>

        <div class="row blogs mb-3">
            <div class=" col-lg-12 col-md-12 col-sm-12" style="padding: 10px;">
                <img src="<%= blog.img %> " class="" alt="..." style="height: 500px; width: 100%;">
              </div>
              
                <div class=" col-lg-12 col-md-12 col-sm-12">
                  <h1 class="card-title"><%= blog.title %> </h1>
                  <h5 class="card-subtitle mb-2 text-muted"><%= blog.date %> </h5>
                  <p class="card-text"><%= blog.desc %> </p>
                  <div class="mt-3">
                    <form action="/blogs/<%=blog._id%>/edit" method="GET">
                        <button class="btn btn-primary">Edit</button>
                  </form>
                  </div>
                  <div class="mt-3">
                    <form action="/user/<%= currentUser._id %>/myblog/<%=blog._id%>?_method=DELETE" method="POST">
                        <button class="btn btn-danger">Delete</button>
                    </form>
                  </div>
                </div>
        </div>


        <div class="row mt-3">
          <!-- <h1>Leave a Review</h1>
          <form class="validated-form" action="/blogs/<%=blog._id%>/review" method="POST" style="max-width:70%;" novalidate>
              <div class="mb-3">
                  <label for="rating" class="form-label">Rating</label>
                  <input type="range" class="form-range" min="0" max="5" name="rating" id="rating">
              </div>
          
              <div class="mb-3">
                  <label for="desc" class="form-label">Description</label>
                  <textarea class="form-control" id="desc" cols="35" rows="3" name="comment" required></textarea>
                  <div class="valid-feedback">
                    Looks good!
                  </div>
                </div>
             
              <button type="submit" class="btn btn-success">Submit</button>
            </form> -->

            <div class="mb-3 mt-3">
              <%for(let review of blog.reviews){%>
                  <div class="card mb-3">
                      <div class="card-header">
                        User
                      </div>
                      <div class="card-body">
                        <h5 class="card-title">Rating :<%=review.rating%></h5>
                        <p class="card-text"><%=review.comment%></p>
                      </div>
                    </div>
  
              <%}%>
          </div>

      </div>


    </div>


<%- include("../partials/footer")%>
